import { component$ } from "@builder.io/qwik";
import styles from "./style.module.scss";

export const Callme = component$(() => {
  return (
    <div class={styles.callme}>
      <button>0546144870</button>
      <button>W</button>
    </div>
  );
});
